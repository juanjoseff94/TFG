import { CandidaturaFilterPipe } from './candidatura-filter.pipe';

describe('CandidaturaFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CandidaturaFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
