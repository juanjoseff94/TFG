import { ExperienciaFilterPipe } from './experiencia-filter.pipe';

describe('ExperienciaFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ExperienciaFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
