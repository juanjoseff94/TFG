import { FilterGeneralPipe } from './filter-general.pipe';

describe('FilterGeneralPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterGeneralPipe();
    expect(pipe).toBeTruthy();
  });
});
