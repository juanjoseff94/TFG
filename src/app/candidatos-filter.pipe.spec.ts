import { CandidatosFilterPipe } from './candidatos-filter.pipe';

describe('CandidatosFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CandidatosFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
